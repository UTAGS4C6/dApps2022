/*
 * General file to be imported to every other module in the project, contains
 * a path to the home directory as well as the public key for JSONWebTokens and
 * OAuth.
*/ 
type CONFIG = {
  HOME: "/src/",
  TOKENIZER: "D1h1B1#M#tI8cSoK"
}

export type { CONFIG };